/**
 * DEPRECATED — replaced by MainNavigator.tsx (card-based navigation).
 *
 * The app now uses: Home (category cards) → Category (section cards) → screen,
 * so the bottom tab bar is gone. This file is intentionally left empty rather
 * than deleted so any stale import fails loudly instead of silently rendering
 * an old navigator. Safe to delete from the repo.
 */
export {};
