import PreApproveVisitorPage from '../visitors/PreApproveVisitorPage';

// All roles see the resident experience on mobile — no role branching
export default function MobileVisitorsPage() {
  return <PreApproveVisitorPage />;
}
